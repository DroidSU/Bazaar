package com.bazaar.theme

import androidx.compose.ui.graphics.Color

// --- NEW Primary Palette: Earthy Green ---
val SageGreen = Color(0xFF5E7C7B)      // Primary color, a calming, muted green
val PaleSage = Color(0xFFE4EBEA)       // Primary Container (Light), a very light, almost-white green
val DeepForest = Color(0xFF1E3A39)     // On Primary Container (Light), for text on the light container

// --- NEW Secondary Palette: Warm Amber ---
val WarmAmber = Color(0xFFE59500)      // Secondary color, for highlights and buttons
val LightAmber = Color(0xFFFFF5E0)     // Secondary Container (Light)
val DarkAmber = Color(0xFF4C3100)      // On Secondary Container (Light)

// --- NEW Neutral & Background Palette ---
val OffWhite = Color(0xFFFBFBFB)       // Main background, clean and bright
val LightGray = Color(0xFFF1F1F1)      // Surface color, for cards and sheets
val DarkCharcoal = Color(0xFF2E2E2E)   // Main text color for high contrast
val MediumGray = Color(0xFF757575)     // Secondary text color for hints and subtitles

// --- NEW Error Palette: Desaturated Red ---
val MutedRed = Color(0xFFC05C5C)       // Error color, less aggressive
val LightPink = Color(0xFFFAE8E8)      // Error Container (Light)
val DarkestRed = Color(0xFF5A1C1C)     // On Error Container (Light)

// --- NEW Dark Theme Colors ---
val DarkBackground = Color(0xFF1B1B1B) // Dark theme background
val DarkSurface = Color(0xFF2A2A2A)    // Dark theme cards and sheets
val PaleWhite = Color(0xFFEAEAEA)      // Dark theme primary text

// --- Universal Colors ---
val White = Color(0xFFFFFFFF)
val Black = Color(0xFF000000)
